import Phaser from "phaser";

/**
 * System
 * =========================================================
 * ■ 役割
 * - ゲーム全体設定の定数管理
 * - Phaser起動設定の基準値
 */

const System = {
  CONFIG: {
    // =====================================================
    // ■ Phaser基本設定
    // =====================================================

    TYPE: Phaser.AUTO,
    WIDTH: 1280,
    HEIGHT: 720,

    // =====================================================
    // ■ Arcade Physics
    // =====================================================

    PHYSICS: {
      DEFAULT: "arcade",
      ARCADE: {
        gravity: { y: 900 },
        DEBUG: true,
      },
    },

    // =====================================================
    // ■ プレイヤー設定
    // =====================================================

    PLAYER: {
      MAX_HP: 2,
      PAID_HOLIDAYS: 3,
    },

    // =====================================================
    // ■ ステージ管理
    // =====================================================

    START_STAGE: 1,

    // =====================================================
    // ■ Scene定義
    // =====================================================

    SCENES: {
      TITLE: "TitleScene",
      GAME: "GameScene",
      RESULT: "ResultScene",
    },
  },
};

export default System;