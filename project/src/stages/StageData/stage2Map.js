// Stage2Map.js

const TILE = 32;

const W = 46;
const H = 34;

const ground = [];

// =========================
// ヘルパー（範囲塗りつぶし）
// =========================
function fill(x1, x2, y1, y2) {
  for (let y = y1; y <= y2; y++) {
    for (let x = x1; x <= x2; x++) {
      ground.push({ x, y });
    }
  }
}

// =========================
// 地面生成（あなたの設計そのまま変換）
// =========================

// 最下層
fill(0, 45, 32, 33);
fill(0, 45, 27, 31);

// 上段ブロック帯
fill(0, 16, 23, 26);
fill(20, 45, 23, 26);

// 中央地形
fill(0, 16, 20, 22);
fill(22, 32, 21, 22);
fill(24, 25, 20, 20);
fill(40, 45, 21, 22);

// 底面・壁
fill(0, 45, 0, 1);
fill(0, 6, 2, 2);
fill(38, 43, 2, 4); 

// 中層
fill(0, 5, 3, 4);
fill(9, 36, 11, 13);
fill(40, 41, 13, 14);

// 中段構造
fill(0, 3, 7, 8);
fill(9, 12, 7, 8);
fill(31, 34, 7, 8);
fill(40, 43, 7, 8);

// ギミック帯
fill(0, 3, 9, 10);
fill(9, 34, 9, 10);
fill(42, 43, 9, 10);

// 上層
fill(0, 5, 11, 13);
fill(11, 36, 11, 13);
fill(40, 41, 13, 14);

// プレイヤー帯周辺
fill(0, 1, 14, 15);
fill(11, 41, 14, 15);

// 中間層
fill(0, 1, 16, 17);
fill(9, 21, 16, 17);
fill(33, 39, 16, 17);

// 上部構造
fill(0, 1, 18, 19);
fill(9, 17, 18, 19);
fill(35, 39, 18, 19);

// 補助地形
fill(0, 16, 20, 20);
fill(44, 45, 0, 20);

// =========================
// ステージデータ
// =========================

const Stage2Map = {
  TILE,

  ground,

  // 敵
  enemies: [
    { x: 10, y: 14, type: "noda" },
    { x: 15, y: 14, type: "ueno" },
  ],

  // 回復
  heals: [
    { x: 12, y: 15, type: "isu" },
  ],

  // トラップ
  traps: [
    { x: 9, y: 15, type: "reifuuki" },
  ],

  // プレイヤー
  playerSpawn: {
    x: 1,
    y: 14,
  },

  // ゴール
  goal: {
    x: 42,
    y: 14,
  },
};

export default Stage2Map;